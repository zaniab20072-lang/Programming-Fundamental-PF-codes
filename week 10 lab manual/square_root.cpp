#include<iostream>
#include<cmath>

using namespace std;

int main()
{
    float num, sqrt_root;
    cout<<" Enter a number : ";
    cin>>num;
    cout<<" The square root of "<<num <<" is : "<<sqrt(num);
    return 0;
}